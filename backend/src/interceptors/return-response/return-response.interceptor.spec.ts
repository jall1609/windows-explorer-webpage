import { ReturnResponseInterceptor } from './return-response.interceptor';

describe('ReturnResponseInterceptor', () => {
  it('should be defined', () => {
    expect(new ReturnResponseInterceptor()).toBeDefined();
  });
});
